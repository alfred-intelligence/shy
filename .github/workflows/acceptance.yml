name: Acceptance

on:
  push:
    branches: [main, next]
  pull_request:
  schedule:
    - cron: "0 6 * * 0"   # Sunday 06:00 UTC — catches upstream image drift

permissions:
  contents: read

jobs:
  # Matrix check names are overridden by `name:` so branch protection requires
  # "ubuntu-22.04", "ubuntu-24.04", "debian-12", "fedora-40" (no job-key prefix).
  containers:
    name: ${{ matrix.label }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: ubuntu-22.04
            image: ubuntu:22.04
          - label: ubuntu-24.04
            image: ubuntu:24.04
          - label: debian-12
            image: debian:12
          - label: fedora-40
            image: fedora:40
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      - name: Build shy
        run: |
          mkdir -p dist
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
            go build -o dist/shy ./cmd
      - name: Run acceptance inside ${{ matrix.label }}
        env:
          MATRIX_IMAGE: ${{ matrix.image }}
        run: |
          docker run --rm \
            -v "$PWD:/src" -w /src \
            "$MATRIX_IMAGE" bash -c '
              set -euo pipefail
              if command -v apt-get >/dev/null 2>&1; then
                apt-get update -qq
                apt-get install -y -qq git curl ca-certificates bash >/dev/null
              elif command -v dnf >/dev/null 2>&1; then
                dnf install -y -q git curl ca-certificates bash shadow-utils >/dev/null
              fi
              useradd -m testuser
              chmod -R a+rX /src
              su -s /bin/bash testuser -c "
                set -euo pipefail
                cd /src
                SHY_BIN=/src/dist/shy ./scripts/acceptance-happy-path.sh
                SHY_BIN=/src/dist/shy ./scripts/acceptance-negative-path.sh
              "
            '

  # Emitted context: "macos".
  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      - run: go build -o dist/shy ./cmd
      - run: |
          ./scripts/acceptance-happy-path.sh
          ./scripts/acceptance-negative-path.sh
